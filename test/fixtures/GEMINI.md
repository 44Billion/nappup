# test/fixtures Folder

- Fixtures are file resources such as .txt and .json to be
used as simplified examples of expected data to be fed to the system.
- Fixtures are only used on tests.
- Add/edit files and subdirectories before running tests instead of during runtime.
